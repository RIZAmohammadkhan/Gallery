# Docker Environment Variables
# Copy this file to .env and update the values as needed

# Google AI API Key (required for AI features)
GEMINI_API_KEY=YouRaIkEY

# MongoDB Configuration (these are set in docker-compose.yml)
MONGODB_ROOT_USERNAME=admin
MONGODB_ROOT_PASSWORD=password123
MONGODB_DATABASE=studio-gallery

# Security (using your existing secret)
NEXTAUTH_SECRET=ai-gallery-super-secret-key-2024-production-ready

# Optional: Override default ports
# APP_PORT=9002
# MONGODB_PORT=27017
