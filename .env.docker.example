# =============================================================================
# DOCKER DEPLOYMENT ENVIRONMENT TEMPLATE
# =============================================================================
# SECURITY NOTICE: Copy this file to .env.docker and replace ALL placeholder values
# NEVER commit .env.docker to version control!

# =============================================================================
# CRITICAL SECURITY INSTRUCTIONS:
# =============================================================================
# 1. Copy this file: cp .env.docker.example .env.docker
# 2. Generate secure secrets using: openssl rand -base64 32
# 3. Replace ALL "CHANGE_ME_*" values below
# 4. Verify .env.docker is in .gitignore (it is by default)
# 5. Run deployment with: ./docker-run.sh

# =============================================================================
# AUTHENTICATION SECRETS (CHANGE IMMEDIATELY!)
# =============================================================================
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=CHANGE_ME_IMMEDIATELY_32_CHARS_MIN

# Your production URL (update for your domain)
NEXTAUTH_URL=http://localhost:9002

# =============================================================================
# AI SERVICES CONFIGURATION
# =============================================================================
# Your Gemini API key from https://aistudio.google.com/app/apikey
GEMINI_API_KEY=YOURGEMINIAPIKEY

# =============================================================================
# DATABASE CONFIGURATION (SECURE CREDENTIALS REQUIRED)
# =============================================================================
# MongoDB admin credentials - MUST be changed from defaults!
# Generate password with: openssl rand -base64 24
MONGO_ROOT_USERNAME=studio_admin
MONGO_ROOT_PASSWORD=CHANGE_ME_SECURE_PASSWORD_32_CHARS_MIN
MONGO_DB_NAME=studio-gallery
MONGO_PORT=27017

# =============================================================================
# APPLICATION CONFIGURATION
# =============================================================================
APP_NAME=AI Gallery Studio
APP_URL=http://localhost:9002
APP_PORT=9002
MAX_FILE_SIZE=10485760

# =============================================================================
# SECURITY VALIDATION CHECKLIST:
# =============================================================================
# Before deploying, ensure you have:
# ✅ Changed NEXTAUTH_SECRET (32+ characters)
# ✅ Changed MONGO_ROOT_PASSWORD (16+ characters) 
# ✅ Added your real GEMINI_API_KEY
# ✅ Updated APP_URL for your domain
# ✅ Verified .env.docker is NOT committed to Git
# ✅ Used HTTPS URLs for production deployment

# =============================================================================
# PRODUCTION DEPLOYMENT NOTES:
# =============================================================================
# - Use environment-specific files (.env.staging, .env.production)
# - Consider external secret management (AWS Secrets Manager, Vault)
# - Enable HTTPS with proper SSL certificates
# - Configure firewall rules and network security
# - Set up monitoring and log aggregation
# - Implement backup and disaster recovery plans
