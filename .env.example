# =============================================================================
# AI GALLERY STUDIO - ENVIRONMENT CONFIGURATION TEMPLATE
# =============================================================================
# SECURITY WARNING: This is a template file. Never commit real secrets!
# Copy this file to .env for local development
# Use .env.docker for Docker deployments with proper secrets

# =============================================================================
# AUTHENTICATION CONFIGURATION
# =============================================================================
# Your application URL - must match exactly where your app is hosted
NEXTAUTH_URL=http://localhost:9002

# NextAuth.js secret - CRITICAL: Generate a secure random string (32+ chars)
# Generate with: openssl rand -base64 32
# Or: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
NEXTAUTH_SECRET=your-secure-random-secret-at-least-32-characters-long

# =============================================================================
# AI SERVICES CONFIGURATION
# =============================================================================
# Google Gemini API key - obtain from Google AI Studio
# Visit: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key-here

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
# For local development (default MongoDB without auth)
MONGODB_URI=mongodb://localhost:27017/studio-gallery

# For MongoDB with authentication (recommended)
# MONGODB_URI=mongodb://username:password@localhost:27017/studio-gallery?authSource=admin

# For production (MongoDB Atlas - recommended for production)
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/studio-gallery?retryWrites=true&w=majority

# =============================================================================
# APPLICATION CONFIGURATION
# =============================================================================
# Maximum file upload size in bytes (default: 10MB)
MAX_FILE_SIZE=10485760

# Application branding
APP_NAME=AI Gallery Studio

# Base URL for the application
APP_URL=http://localhost:9002

# =============================================================================
# DOCKER CONFIGURATION (for docker-compose.yml)
# =============================================================================
# Port mappings for Docker deployment
APP_PORT=9002
MONGO_PORT=27017

# MongoDB credentials for Docker (change these!)
MONGO_ROOT_USERNAME=admin
MONGO_ROOT_PASSWORD=change-me-secure-password
MONGO_DB_NAME=studio-gallery

# =============================================================================
# DEVELOPMENT CONFIGURATION
# =============================================================================
# Node.js SSL Configuration (uncomment if needed for MongoDB Atlas in dev)
# NODE_TLS_REJECT_UNAUTHORIZED=0

# =============================================================================
# SECURITY CHECKLIST:
# =============================================================================
# ✅ Generate unique NEXTAUTH_SECRET (32+ characters)
# ✅ Use strong MongoDB credentials
# ✅ Never commit real API keys or secrets
# ✅ Use different secrets for different environments
# ✅ Rotate secrets regularly
# ✅ Use HTTPS in production
# ✅ Consider using a secrets management service for production